# GENERATED by UDAP Build Project from .udap/pipeline.yaml — do not
# edit by hand; change the spec and re-render.
name: deploy
'on':
  workflow_dispatch: {}
jobs:
  provision:
    runs-on: ubuntu-latest
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_ssh_public_key: ${{ secrets.SSH_PUBLIC_KEY }}
      TF_VAR_region: us-east-1
    timeout-minutes: 120
    steps:
    - uses: actions/checkout@v4
    - name: Bootstrap and sync springboot-cicd-template repo
      run: "set -euo pipefail\nTEMPLATE_REPO=\"${REPO_OWNER}/springboot-cicd-template\"\
        \necho \"=== Bootstrap: $TEMPLATE_REPO ===\"\n\n# Create repo if it does not\
        \ exist\nif ! gh api \"/repos/${TEMPLATE_REPO}\" --silent 2>/dev/null; then\n\
        \  echo \"Creating $TEMPLATE_REPO ...\"\n  gh api --method POST /user/repos\
        \ \\\n    -f name=springboot-cicd-template \\\n    -f description=\"Enterprise\
        \ reusable CI/CD pipeline for Spring Boot apps\" \\\n    -F private=false\
        \ \\\n    -F auto_init=true\n  echo \"Waiting 15s for GitHub to initialise\
        \ repo...\"\n  sleep 15\nelse\n  echo \"Repo $TEMPLATE_REPO already exists.\"\
        \nfi\n\n# Clone the template repo and sync cicd-template/ into it\nTMPDIR=$(mktemp\
        \ -d)\ngit clone \"https://${GH_TOKEN}@github.com/${TEMPLATE_REPO}.git\" \"\
        $TMPDIR\"\nmkdir -p \"$TMPDIR/.github/workflows\"\n\ncp cicd-template/.github/workflows/enterprise-pipeline.yml\
        \ \\\n   \"$TMPDIR/.github/workflows/enterprise-pipeline.yml\"\ncp cicd-template/README.md\
        \ \"$TMPDIR/README.md\"\n\ncd \"$TMPDIR\"\ngit config user.email \"cicd-bootstrap@springboot-api\"\
        \ngit config user.name \"springboot-api CI\"\ngit add -A\n\nif git diff --cached\
        \ --quiet; then\n  echo \"Template repo already up to date \u2014 no commit\
        \ needed.\"\nelse\n  git commit -m \"chore: sync enterprise pipeline from\
        \ springboot-api@${{ github.sha }}\"\n  git push origin main\n  echo \"Template\
        \ repo updated.\"\nfi\n"
      env:
        GH_TOKEN: ${{ secrets.CICD_TEMPLATE_TOKEN }}
        REPO_OWNER: ${{ github.repository_owner }}
    - id: dispatch
      name: Dispatch enterprise pipeline to springboot-cicd-template
      run: "set -euo pipefail\necho \"=== Two-Repo Enterprise CI/CD Pattern ===\"\n\
        echo \"App repo  : $APP_REPO\"\necho \"Template  : ${REPO_OWNER}/springboot-cicd-template\"\
        \necho \"Commit SHA: $COMMIT_SHA\"\n\n# Brief pause to ensure workflow file\
        \ is indexed by GitHub after sync\nsleep 10\n\nHTTP_STATUS=$(gh api \\\n \
        \ --method POST \\\n  -H \"Accept: application/vnd.github+json\" \\\n  \"\
        /repos/${REPO_OWNER}/springboot-cicd-template/actions/workflows/enterprise-pipeline.yml/dispatches\"\
        \ \\\n  -f ref=main \\\n  -f \"inputs[app_repo]=${APP_REPO}\" \\\n  -f \"\
        inputs[app_ref]=${COMMIT_SHA}\" \\\n  -f \"inputs[java_version]=17\" \\\n\
        \  -f \"inputs[image_name]=${REPO_OWNER}/springboot-api\" \\\n  -f \"inputs[aws_region]=us-east-1\"\
        \ \\\n  -f \"inputs[instance_type]=t3.small\" \\\n  -f \"inputs[health_endpoint]=/actuator/health\"\
        \ \\\n  -f \"inputs[run_perf_tests]=true\" \\\n  -f \"inputs[owasp_cvss_threshold]=9\"\
        \ \\\n  --silent -o /dev/null -w \"%{http_code}\" 2>/dev/null || echo \"000\"\
        )\n\necho \"Dispatch HTTP status: $HTTP_STATUS\"\nif [ \"$HTTP_STATUS\" =\
        \ \"204\" ]; then\n  echo \"Enterprise pipeline dispatched successfully.\"\
        \nelse\n  echo \"::error::Dispatch failed (HTTP $HTTP_STATUS). Check CICD_TEMPLATE_TOKEN\
        \ has actions:write on springboot-cicd-template.\"\n  exit 1\nfi\n\necho \"\
        dispatch_time=$(date -u +%Y-%m-%dT%H:%M:%SZ)\" >> \"$GITHUB_OUTPUT\"\n"
      env:
        GH_TOKEN: ${{ secrets.CICD_TEMPLATE_TOKEN }}
        APP_REPO: ${{ github.repository }}
        COMMIT_SHA: ${{ github.sha }}
        REPO_OWNER: ${{ github.repository_owner }}
    - name: Poll enterprise pipeline to completion
      run: "set -euo pipefail\necho \"Waiting 45s for dispatched run to appear in\
        \ GitHub...\"\nsleep 45\n\nMAX_WAIT=5400\nWAITED=0\nINTERVAL=30\n\nwhile [\
        \ \"$WAITED\" -lt \"$MAX_WAIT\" ]; do\n  RUN_JSON=$(gh api \\\n    -H \"Accept:\
        \ application/vnd.github+json\" \\\n    \"/repos/${REPO_OWNER}/springboot-cicd-template/actions/workflows/enterprise-pipeline.yml/runs?per_page=1\"\
        \ \\\n    2>/dev/null || echo '{\"workflow_runs\":[]}')\n\n  STATUS=$(echo\
        \ \"$RUN_JSON\" | python3 -c \\\n    \"import sys,json; r=json.load(sys.stdin)['workflow_runs'];\
        \ print(r[0]['status'] if r else 'none')\" 2>/dev/null || echo \"none\")\n\
        \  CONCLUSION=$(echo \"$RUN_JSON\" | python3 -c \\\n    \"import sys,json;\
        \ r=json.load(sys.stdin)['workflow_runs']; print(r[0]['conclusion'] or 'pending'\
        \ if r else 'none')\" 2>/dev/null || echo \"none\")\n  RUN_URL=$(echo \"$RUN_JSON\"\
        \ | python3 -c \\\n    \"import sys,json; r=json.load(sys.stdin)['workflow_runs'];\
        \ print(r[0]['html_url'] if r else '')\" 2>/dev/null || echo \"\")\n\n  echo\
        \ \"[${WAITED}s] status=${STATUS} conclusion=${CONCLUSION} url=${RUN_URL}\"\
        \n\n  if [ \"$STATUS\" = \"completed\" ]; then\n    if [ \"$CONCLUSION\" =\
        \ \"success\" ]; then\n      echo \"Enterprise pipeline SUCCEEDED: $RUN_URL\"\
        \n      exit 0\n    else\n      echo \"::error::Enterprise pipeline FAILED\
        \ (conclusion=${CONCLUSION}): $RUN_URL\"\n      exit 1\n    fi\n  fi\n\n \
        \ sleep \"$INTERVAL\"\n  WAITED=$((WAITED + INTERVAL))\ndone\n\necho \"::error::Timeout:\
        \ enterprise pipeline did not complete within ${MAX_WAIT}s\"\nexit 1\n"
      env:
        GH_TOKEN: ${{ secrets.CICD_TEMPLATE_TOKEN }}
        REPO_OWNER: ${{ github.repository_owner }}
  configure:
    runs-on: ubuntu-latest
    needs:
    - provision
    timeout-minutes: 5
    steps:
    - uses: actions/checkout@v4
    - name: "Configure complete \u2014 handled by enterprise-pipeline.yml"
      run: 'echo "All configuration (Docker, nginx, app container) was performed"

        echo "by ansible-configure stage inside springboot-cicd-template."

        echo "This stage satisfies the platform provision -> configure -> verify backbone."

        '
  verify:
    runs-on: ubuntu-latest
    needs:
    - configure
    - provision
    timeout-minutes: 10
    steps:
    - uses: actions/checkout@v4
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_wrapper: false
    - name: Terraform init (read state)
      run: "terraform init -input=false -reconfigure \\\n  -backend-config=\"bucket=${{\
        \ secrets.TF_STATE_BUCKET }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME\
        \ }}/terraform.tfstate\" \\\n  -backend-config=\"region=us-east-1\"\n"
      working-directory: infra
      env:
        AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
        AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        AWS_REGION: us-east-1
    - id: get_ip
      name: Get instance IP from terraform output
      run: echo "instance_ip=$(terraform output -raw instance_ip)" >> "$GITHUB_OUTPUT"
      working-directory: infra
      env:
        AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
        AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        AWS_REGION: us-east-1
    - name: Final health check with retries
      run: "echo \"Confirming app health after full enterprise pipeline run...\"\n\
        curl --fail --retry 12 --retry-delay 15 --retry-all-errors \\\n  http://${{\
        \ steps.get_ip.outputs.instance_ip }}/actuator/health\n"
